import styled from "styled-components";

export const StyledLayout = styled.div`
  .mask {
    width: 100%;
    min-height: 100vh;
    background: rgba(0, 0, 0, 0.6);
    .inner {
      width: 50vw;
      margin: 0 auto;
    }
  }
`;
