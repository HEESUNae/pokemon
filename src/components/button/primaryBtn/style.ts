import styled from "styled-components";

export const StyledPrimaryBtn = styled.button`
  font-size: 15px;
  color: #fff;
  padding: 15px 30px;
  border-radius: 8px 16px;
  position: relative;
  font-weight: 600;
  background: linear-gradient(45deg, #0d1330, #1b1f4f);
  transition: 0.2s;
`;
